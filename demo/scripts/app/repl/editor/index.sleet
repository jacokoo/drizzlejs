#! drizzle

module(code) > view-editor(code)

script.
    export default {
        items: { views: ['view-editor'] },

        store: {
            models: {
                code: () => ''
            },

            actions: {
                update ({detail}) {
                    this.fire('change', {code: detail})
                }
            }
        }
    }
