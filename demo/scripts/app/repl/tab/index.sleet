#! drizzle

module(tabs) > .tabs
    ul > view-tab(tabs current)

script.
    export default {
        items: { views: ['view-tab'] },
        store: {
            models: {
                tabs: () => [],
                current: () => 1
            }
        },

        rendered () {
            console.log(this.get(), 'index')
        }
    }
