// Generated by CoffeeScript 1.7.1
(function() {
  define(['jquery', 'underscore'], function($, _) {
    var config, defaults, key, merged, value, _ref;
    defaults = {
      development: true,
      cache: true,
      scriptRoot: 'app',
      urlRoot: '',
      urlSuffix: '',
      attributesReferToId: ['for', 'data-target', 'data-parent'],
      fileNames: {
        module: 'index',
        templates: 'templates',
        view: 'view-',
        template: 'template-',
        handler: 'handler-',
        model: 'model-',
        collection: 'collection-',
        router: 'router'
      }
    };
    config = require('config');
    merged = _.extend({}, defaults, config);
    _ref = defaults.fileNames;
    for (key in _ref) {
      value = _ref[key];
      if (!(key in merged.fileNames)) {
        merged.fileNames[key] = value;
      }
    }
    return merged;
  });

}).call(this);
