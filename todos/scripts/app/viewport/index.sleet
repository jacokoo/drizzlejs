#! drizzle
module
    div
        region

script.
    export default {
        items: {
            modules: { 'todo-app': 'todos' }
        },

        routes: {
            '/todos1': {
                ref: 'todo-app',
                region: 'region-name'
            },

            '/todos': 'todo-app'   // use default region
        }
    }
